{{- /*
	vim:ft=yaml.gotexttmpl
*/ -}}

theme: {{ (print (.theme.name | lower) "-" (.theme.variant | lower)) }}
layout: horizontal
response:
  prettify_json: false
heading:
  visible: true
  show_host: false
pager: less -R --tabs={{ .env.tabs }}
