#!/usr/bin/env sh
# vim:ft=go

cd "$HOME" || exit 1

{{ if eq .chezmoi.os "darwin" }}
echo "Install Homebrew packages"
# Brewfile hash: {{ include ".packagefiles/Brewfile" | sha256sum }}
	if [[ -f {{ joinPath .chezmoi.sourceDir ".packagefiles/Brewfile" | quote }} ]] && [[ -s {{ joinPath .chezmoi.sourceDir ".packagefiles/Brewfile" | quote }} ]]; then
		export PATH="$HOME"/.homebrew/bin:"$PATH"
		brew bundle --no-lock --force --file={{ joinPath .chezmoi.sourceDir ".packagefiles/Brewfile" | quote }} || true
		brew cleanup --quiet --prune=30 || true
	fi
{{ end }}
