#!/usr/bin/env sh
# vim:ft=bash.gotexttmpl

cd "$HOME" || exit 1

{{ if and (stat (joinPath .chezmoi.sourceDir ".packagefiles/krew.packages")) (gt (stat (joinPath .chezmoi.sourceDir ".packagefiles/krew.packages")).size 0) }}
	# krew.packages hash: {{ include ".packagefiles/krew.packages" | sha256sum }}
	{{ template "path.tmpl" . }}
	if command -v krew >/dev/null 2>&1; then
		echo "Krew. Install kubectl krew packages"
		export KREW_ROOT="$HOME/.local/krew"
		krew install <{{ joinPath .chezmoi.sourceDir ".packagefiles/krew.packages" | quote }}
	fi
{{ end }}
