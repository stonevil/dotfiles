# vim:ft=toml

{{ range $k, $v := $.zsh.pluginsList }}
[".local/zsh/plugins/{{ $k }}"]
	type = "git-repo"
	refreshPeriod = "168h"
	executable = true
	url = "https://github.com/{{ $v }}/{{ $k }}.git"
{{ end }}
