{{- /*
vim:ft=bash.gotexttmpl
*/ -}}

{{ template "path.tmpl" . }}

if command -v nvim >/dev/null 2>&1; then
	echo "Nvim. Lazy plugin and dependencies"
	nvim --headless +AstroUpdate "+Lazy! sync" +qall! || exit 1
fi
