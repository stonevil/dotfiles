{{- /*
vim:ft=bash.gotexttmpl
*/ -}}

{{ if eq .chezmoi.os "linux" }}
	{{ if eq .chezmoi.osRelease.id "arch" }}
sudo sh -c 'cat > /etc/profile.d/editor.sh' << EOF
#!/bin/sh

export EDITOR=vim
export SYSTEMD_EDITOR=vim
EOF
	{{ end }}
{{ end }}
