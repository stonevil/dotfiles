#!/usr/bin/env bash
# vim:ft=bash

trap "exit" INT

media_url="$@"

if [[ -z "${media_url}" ]]; then
	read -p "Please provide URL: " media_url
fi

yt_user_agent={{ .web.ytUserAgent | quote }}

cd {{ (joinPath .chezmoi.homeDir "Downloads") | quote }} || exit 1

yt_title=$(yt-dlp --skip-download --get-title --no-warnings "$media_url" | sed 2d)
printf "%s\n" "Download media: $yt_title"

media_number=$(yt-dlp --no-warnings --quiet --user-agent "$yt_user_agent" --list-formats "$media_url" | fzf | awk '{print $1}')
yt-dlp --no-warnings --user-agent "$yt_user_agent" --format "$media_number" --embed-metadata --embed-thumbnail --recode-video mp4 --audio-format m4a --merge-output-format mp4 "$media_url"
